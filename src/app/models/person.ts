export class Person {
    name: string;
    salary: number;

    constructor(name: string, salary: number) {
        {
            this.name = name;
            this.salary = salary;
        }
    }
}