export class Person {
    id: string;
    name: string;
    salary: number;

    constructor(id: string, name: string, salary: number) {
        {
            this.id = id;
            this.name = name;
            this.salary = salary;
        }
    }
}